// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "ResourceHelper",
    products: [
        .library(
            name: "ResourceHelper",
            targets: ["ResourceHelper"])
    ],
    dependencies: [],
    targets: [
        .target(
            name: "ResourceHelper",
            dependencies: [])
    ]
)
